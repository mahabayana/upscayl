# Step 005: Windows Arm64 porting audit

- **Date:** `2026-09-14`
- **Objective:** Complete a read-only Windows Arm64 porting audit, attempt the
  separately approved isolated EasyWoS scanner setup, preserve the resulting
  evidence, and close Step 5 through methodology-only analysis after the
  scanner dependency gate failed.
- **Agent:** GitHub Copilot CLI.
- **Skill:** `upshift64-port`, updated by the approved closeout.
- **Model:** GPT-5.6 Sol (`gpt-5.6-sol`).
- **Commit:** Not created.
- **Pull request:** Not created.
- **CI:** Not run.

## Approval record

The available conversation records these exact owner messages:

- `"okk approved"` approved the presented Step 5 read-only audit scope. It did
  not approve implementation.
- The owner later explicitly approved the exact pinned, isolated EasyWoS setup
  and scan attempt under the session evidence root. That approval included a
  hard stop if `python-magic` or any dependency failed.
- `"I ll go with teh recommended"` selected the recommended methodology-only
  completion after the scanner stopped at its dependency gate. It did not
  authorize a libmagic workaround, Docker, or product changes.
- `"ok sure"` approved the presented detailed Step 5 closeout, including this
  provenance record, the durable audit, skill update, plan update, and custom
  auditor agent.

These quotations are recorded in their action context and do not imply
approval of Step 6, remediation choices, builds, or repository changes beyond
the five closeout files.

## Audit scope and pinned repositories

| Component | Path/repository | Branch | Revision |
| --- | --- | --- | --- |
| Application | `C:\upscayl`, `upscayl/upscayl` | `hackathon/upshift64-win-arm64` | `a00d55fee90e0f9435d5eaa86e76700df8199af8` |
| Backend | `C:\upscayl-ncnn`, `upscayl/upscayl-ncnn` | `hackathon/upshift64-win-arm64` | `0beb39028a0ddd83250e845b4c3333c0675e3b97` |
| libwebp | backend submodule | backend-pinned | `8ea81561d2fdd382da60f57958741a7c23a18eb6` |
| ncnn | backend submodule | backend-pinned | `6125c9f47cd14b589de0521350668cf9d3d37e3c` |
| glslang | nested ncnn submodule | backend-pinned | `4afd69177258d0636f78d2c4efb823ab6382a187` |
| pybind11 | nested ncnn submodule | backend-pinned | `70a58c577eaf067748c2ec31bfd0b0a614cffba6` |

The read-only task covered:

1. CMake and CI/release architecture restrictions, including `-A x64`.
2. Visual Studio/MSVC ARM64 compiler, Windows SDK, CMake generator, and
   `windows-11-arm` runner readiness.
3. NCNN target detection, ARM/NEON guards, Vulkan options, shaders, and CPU
   fallback.
4. Vulkan host tools versus ARM64 target headers, import libraries, and runtime.
5. libwebp Windows ARM64 compilation, NEON/SIMD dispatch, and x86 exclusion.
6. WIC, COM, filesystem, Unicode paths, and other `_WIN32` source paths.
7. OpenMP discovery/linking and ARM64 `vcomp` runtime/distribution.
8. Static and dynamic backend dependency closure.
9. Backend/application artifact naming, staging, updater collisions, and
   native helpers.

## Official sources

- Qualcomm EasyWoS: <https://github.com/qualcomm/EasyWoS>
- EasyWoS pinned commit:
  <https://github.com/qualcomm/EasyWoS/commit/e096b5b95a25c18220be93f743a668846ef3481b>
- Pinned standalone scanner documentation:
  <https://github.com/qualcomm/EasyWoS/blob/e096b5b95a25c18220be93f743a668846ef3481b/scanner/cpp/README.md>
- CMake Visual Studio 17 2022 generator:
  <https://cmake.org/cmake/help/latest/generator/Visual%20Studio%2017%202022.html>
- GitHub-hosted runner reference:
  <https://docs.github.com/en/enterprise-cloud@latest/actions/reference/runners/github-hosted-runners>
- Microsoft OpenMP option reference:
  <https://learn.microsoft.com/en-us/cpp/build/reference/openmp-enable-openmp-2-0-support?view=msvc-170>

## Read-only inspections

The agent inspected CMake files, workflow definitions, pinned NCNN and libwebp
sources, WIC/Windows code paths, Electron packaging and process handoff, and the
application lockfile. Local read-only checks included:

- `git rev-parse`, branch, status, and recursive submodule status;
- Visual Studio inventory with `vswhere`;
- filesystem checks for `Hostx64\arm64\cl.exe`, ARM64 Windows SDK import
  libraries, and ARM64 MSVC OpenMP libraries/redistributables;
- PE machine-type and SHA256 inspection;
- `dumpbin /headers /dependents` on the bundled backend and `vcomp` DLLs.

Observed toolchain facts included Visual Studio Enterprise 2022 17.14.40,
MSVC `14.44.35207\bin\Hostx64\arm64\cl.exe`, ARM64
`windowscodecs.lib` in Windows SDK 10.0.22621.0 and 10.0.26100.0, ARM64
`vcomp.lib`, and an ARM64 release `vcomp140.dll` redistributable. The Community
installation did not expose the ARM64 cross compiler in the inspected path.

The bundled `upscayl-bin.exe`, `vcomp140.dll`, and `vcomp140d.dll` each
reported PE machine `0x8664` (x64). The current x64 backend directly imports
`vulkan-1.dll`, `ole32.dll`, `KERNEL32.dll`, `OLEAUT32.dll`, and
`VCOMP140.DLL`.

## Isolated EasyWoS attempt

- **Root:**
  `<private-evidence-root>\easywos`
- **Official remote:** `https://github.com/qualcomm/EasyWoS.git`
- **Verified HEAD:** `e096b5b95a25c18220be93f743a668846ef3481b`
- **Python:** CPython 3.11.9, AMD64
- **Environment:** isolated venv under `easywos\venv`; pip cache and
  `TEMP`/`TMP` under the same root.

Only `scanner\cpp\requirements.txt` was installed with pip's `--isolated` and
`--require-virtualenv` flags. The resulting freeze was:

```text
Jinja2==3.1.6
MarkupSafe==3.0.3
progressbar33==2.4
pycryptodome==3.23.0
python-magic==0.4.27
ruamel.yaml==0.19.1
```

`pip check` returned `No broken requirements found.` Imports of `jinja2`,
`progressbar`, `ruamel.yaml`, and `Crypto` succeeded. The mandatory
`python-magic` import failed:

```text
ImportError: failed to find libmagic.  Check your installation
```

The process exited 1. The approved stop condition was applied immediately:
scanner help was not run, the scanner was not run, and no JSON report or
scanner findings were generated. No alternative package, libmagic binary,
Docker image, or workaround was installed or used. The evidence directory was
retained.

## EasyWoS evidence hashes

| Evidence | SHA256 |
| --- | --- |
| `logs\source-head.txt` | `5DCBC04E7F911C465017D283091888883AFF3A02E7B47D2E32A0B25CD2A05617` |
| `source\EasyWoS\scanner\cpp\requirements.txt` | `3DD3903BA09A575736413B4689FFBFEFFECAB1C83F93C36FD1B5208BCB272F73` |
| `logs\pip-freeze.txt` | `0279645613DE981F2FFEC9D2D61187BFDA657698E01526535BD6C9099F40E753` |
| `logs\pip-check.txt` | `672AEC65A45BED50E70229C70934A7EEBF0462595C459B83023027645D57CCCF` |
| `logs\core-imports.txt` | `724FC2E4AAC8A8D80E7B69AFE4E348A21C15419EFB791DAA802FE23FCFA9932C` |
| `logs\python-magic-import.txt` | `2720ED79AEBD2CA2CDAE8A593D98E04A1BC0C044BEAA80CF1A59FEEB5B0BB610` |

`scanner-help.txt`, `scanner-run.txt`, and
`outputs\upscayl-ncnn-windows-arm64.json` are absent because the dependency
gate stopped execution before those stages.

## Generated decision

Close Step 5 using the pinned EasyWoS methodology plus direct repository and
toolchain inspection. Do not represent any issue in the audit as an EasyWoS
scanner finding. Carry forward a CI-only first implementation proposal:
`C:\upscayl-ncnn\.github\workflows\CI.yml`. That Step 6 proposal is not
approved or started.

The complete generated audit is
`docs\upshift64\arm64-port-audit.md`.

## Repository integrity and validation

Before and after the isolated setup:

- the application remained at
  `a00d55fee90e0f9435d5eaa86e76700df8199af8` on
  `hackathon/upshift64-win-arm64`;
- its pre-existing untracked documentation/skill/plan files remained the only
  working-tree entries;
- the backend remained clean at
  `0beb39028a0ddd83250e845b4c3333c0675e3b97` on
  `hackathon/upshift64-win-arm64`;
- all four recursive submodule revisions remained unchanged.

This closeout changes only the five owner-approved application documentation,
skill, plan, and custom-agent paths. `git diff --check` is the required
formatting validation. No backend file, product source, workflow, dependency,
or EasyWoS evidence file is modified by the closeout.

## Limitations

- EasyWoS scanner execution was not reached; there are no raw scanner counts,
  true-positive classifications, false-positive classifications, or scanner
  report.
- No CMake configuration, compilation, link, CI job, packaging operation, or
  Windows Arm64 test was run.
- No physical Snapdragon X or ARM64 Vulkan runtime was inspected.
- Installed toolchain files demonstrate local capability, not successful
  project integration.
- Candidate remediations and alternatives remain proposals requiring separate
  owner approval.
